# src/Utils/CMakeLists.txt

# Agrega el subdirectorio Logger. CMake buscará un CMakeLists.txt dentro de él.
add_subdirectory(Logger)

# Si tuvieras más subdirectorios en Utils, irían aquí:
# add_subdirectory(Math)